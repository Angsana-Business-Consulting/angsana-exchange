import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function ManagedListsPage() {
  return (
    <PlaceholderPage
      title="Managed Lists"
      description="Service types, sectors, geographies, title bands, and company sizes configuration will appear here."
    />
  );
}
